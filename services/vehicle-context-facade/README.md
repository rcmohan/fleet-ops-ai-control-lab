# Vehicle Context Facade

Read-only MCP/UCP-facing composition boundary for the interface described in `OVERVIEW.md` section 5.1. It calls authoritative domain services and composes results in memory; it owns no master-data records.

